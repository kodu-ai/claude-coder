import { DEFAULT_MAX_REQUESTS_PER_TASK } from "../shared/Constants";

export { DEFAULT_MAX_REQUESTS_PER_TASK };

// Add any other constants specific to the agent here
export const API_RETRY_DELAY = 1000; // milliseconds
export const COMMAND_OUTPUT_DELAY = 100; // milliseconds